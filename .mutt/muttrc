set sort = reverse-date-received
unset mark_old
auto_view text/html
set tilde
set pager_stop
set editor = vim
set user_agent
set edit_headers

set from = "kopeikin88@gmail.com"
set realname = "Alexey Kopeikin"

set folder = "imaps://imap.gmail.com:993"
set imap_user = "$from"
set spoolfile = "+INBOX"
set postponed = "+[Gmail]/Drafts"
set record = "+[Gmail]/Sent Mail"
set trash = "+[Gmail]/Trash"

set header_cache = "~/.mutt/cache"

set smtp_url = "smtp://smtp.gmail.com:587"
unset copy
